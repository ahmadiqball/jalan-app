# pnpm runs a deps-status check before `run` scripts (nuxt dev) and fails it
# on ignored build scripts. Our build scripts are approved via
# pnpm-workspace.yaml onlyBuiltDependencies; disable the pre-run check so dev
# doesn't invoke `pnpm install` on every start.
verify-deps-before-run=false
